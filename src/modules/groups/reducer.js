import * as types from './actionTypes';

const INITIAL_STATE = {
  error: '',
  loading: ''
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
